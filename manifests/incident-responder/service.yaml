apiVersion: v1
kind: Service
metadata:
  name: incident-responder
  namespace: monitoring
spec:
  selector:
    app: incident-responder
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP